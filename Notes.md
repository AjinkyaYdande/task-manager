#React Router DOM 
- Import BrowserRouter in App.jsx wrap <Routes/> and <Route/> in <BrowserRouter/>  
- Utilize useNavigate hook
- Without BrowserRouter useNavigate hook will not work.

#All tasks
- Use virtualization to load only data as per screen size

#Context API [Practice]
- Create Context
- Create Provider also pass state
- Use Context

#Backend
 - Express : create APIs
 - cors → allow frontend (React) to call backend
 - mongoose → connect to MongoDB + schemas
 - jsonwebtoken (JWT) → login token
 - bcryptjs → password hashing
 - dotenv → store secrets (DB URL, JWT secret)